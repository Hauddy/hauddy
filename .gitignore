# Dependencies
node_modules/

# Build output
dist/
release/
*.tsbuildinfo

# Local data & runtime state
data/
.hauddy/
.hauddy-platform-data/
.wrangler/

# Secrets & keys
*.key
.env
.env.*
.dev.vars
.dev.vars.*

# Logs
*.log

# OS / editor
.DS_Store

# Design-tool scratch exports (not part of the buildable project)
*.dc.html
support.js

# Playwright MCP session snapshots
.playwright-mcp/

# Marketing / brand assets (worked on separately, root-level only)
/*.png
/*.html

# Internal scripts not for public repo
scripts/send-alpha-invites.mjs

# Marketing animation assets — worked on separately, not part of the build
animation/

# Internal planning docs — not for public consumption
docs/app-ux-plan.md
docs/cloudflare-platform-port-plan.md
docs/deploy-alpha.md

# Daemon bundle (build artifact — regenerated by bundle-daemon.mjs)
packages/desktop/daemon-bundle/
